// The Twitter/X card reuses the exact same design as the Open Graph card.
// Re-exporting keeps one source of truth for the 1200x630 image.
export { default, alt, size, contentType } from "./opengraph-image";
